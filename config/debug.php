<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Capture debug messages
    |--------------------------------------------------------------------------
    |
    | When enabled, all debug messages will be captured and displayed
    | at the end of the page altogether. This keeps the HTML clean but
    | you cannot see the direct spot in the HTML generation where the
    | debug message was created.
    |
    | When disabled, debug messages will be displayed in the console
    | as soon as they are created.
    |
    | Default: false
    */
    'capture' => false,
];
